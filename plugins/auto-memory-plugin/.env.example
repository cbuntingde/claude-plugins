# Auto Memory Plugin Environment Variables

# Claude Code Environment Variables
# These are automatically set by Claude Code

# Session ID for tracking context across sessions
# Format: internal-provider-id-session-id
CLAUDE_SESSION_ID=unknown

# Project name for context categorization
CLAUDE_PROJECT_NAME=unknown

# Plugin root path (for internal use)
# Automatically set by Claude Code: ${CLAUDE_PLUGIN_ROOT}

# Optional: Custom memory data directory (relative to plugin root)
# AUTO_MEMORY_DATA_DIR=auto-data

# Optional: Maximum age of memories in days for pruning
# AUTO_MEMORY_MAX_AGE_DAYS=90

# Optional: Minimum utility threshold for pruning (0-1)
# AUTO_MEMORY_MIN_UTILITY=0.1

# Optional: Maximum memories per session
# AUTO_MEMORY_SESSION_LIMIT=100

# Optional: Lambda value for retrieval (0=min_similarity, 1=max_utility)
# AUTO_MEMORY_LAMBDA=0.5

# Optional: Top-K results for retrieval
# AUTO_MEMORY_TOP_K=5